import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    text: {
        color: "#fff",
        textAlign: "center",
        fontSize: 28,
        fontWeight: "bold",
        padding: 14,
        borderColor: "#fff",
        borderWidth: 2,
        maxWidth: 300,
        marginTop: 25
    }
})
